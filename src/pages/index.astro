---
// Static root: always lands on the Czech site. English is only ever
// reached via the header language toggle, never auto-selected — so this
// is an unconditional redirect, same stub pattern as old-URL redirects.
import Redirect from '../components/Redirect.astro';
---

<Redirect to="cs/" />
